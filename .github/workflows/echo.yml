name: echo-public-shim
on:
  workflow_call:
    secrets:
      test_secret:
        required: true
jobs:
  passthrough:
    runs-on: ubuntu-latest
    steps:
      - name: report
        env:
          SECRET: ${{ secrets.test_secret }}
        run: echo "public-shim: received secret, length=${#SECRET}"
